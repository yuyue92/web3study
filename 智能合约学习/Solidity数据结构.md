solidity数据结构：
- uint/int： 用于表示无符号和有符号整数，常用来存储数量、索引；
- address：用来存储以太坊地址，存储用户钱包地址或合约地址；
- string、bytes：用于存储文本数据，string更高级支持更多操作，bytes可以有效处理二进制数据；
- array用来存储相同类型的元素列表，可以是固定长度的数组，或者动态长度的数组；
- mapping：用于创建键值对映射，存储关联数据；
- struct 用于创建自定义数据结构，可以包含不同类型的字段；
- enum 用于创建枚举，限制变量的取值范围；
