**一句话理解 DeFi/DApp 的整体结构**
- 一个完整的DeFi、DApp通常由“区块链网络 + 智能合约+ 前端+ 后端服务（若干种） +数据索引/查询” 组成；
- 区块链网络：像以太坊，Arbitrum,Polygon, 一个DeFi应用往往会部署到多个链上（多网络多版本）
- 智能合约：核心的业务逻辑（借贷、交易、清算、质押，，，）都写在合约里面，运行在链上，公开透明但不能主动“定时自己跑”；
- 前端webUI：用户操作界面；
- 钱包wallet：用户身份与签名工具；
- SDK：给前端用的工具包，把“连钱包、查数据、调合约”这些复杂的动作封装成好用的方法。
- 后端服务（不止一种）：用来补齐“链上不擅长/做不了”的事情，比如定时任务、消息队列、数据整理、与传统的系统集成等。
- 索引/查询层：把链上数据整理成前端好查询的状态（subgraph/ 自建后端索引都行）。

关键概念分为三类：
- 链上核心：合约+链
   - blockchain（区块链网络）：应用运行的“平台/环境”， 不同的链就是不同的“部署环境”；
   - smart Contract（智能合约）：应用规则本体，放在链上执行，它负责“该怎么做”，但不负责“什么时候自动触发”；
 
- 链下辅助：帮合约“补能力”的后端；
   - price Oracle（价格预言机）：给合约提供价格数据，价格经常来自链外（交易所/行情源），所以通常需要后端服务把数据喂给链上或者供链上读取；
   - keeper service（任务触发器/执行器）：因为合约不会自己定时跑（比如自动清算、自动结算、定时更新），就需要keeper在合适时间去发起交易 触发合约执行；
 
- 数据查询：让前端“查得快、查的方便”
   - subgraph（子图/索引器）：把链上的事件/状态重新整理成“前端容易查”的结构（比如graphQL查询）；
   - graph node：subgraph运行的环境，会同步区块数据并交给subgraph处理生成索引；
 
 为什么“区块链”应用还需要后端？实际项目粒，需要后端的几个原因：
 - 数据存储、查询与整理：链上读数据成本高、结构不适合复杂查询；后端/索引层可以把数据整理成适合UI的接口；
 - 与传统系统的集成：比如登录体系、风控系统、KYC、中心化数据库、第三方支付、通知等。
 - 与区块链交互的“统一入口”：后端封装合约调用，做成restful API，前端调用像调用普通互联网服务一样（也便于权限、限流、监控）
 - 定时任务/自动化：合约自己不会“到点执行”，keeper/后端来负责触发；
 - 消息队列与异步处理：交易确认、事件监听、通知推送、重试机制都更适合在后端做；
 - 工程化能力：日志、监控、报警、灰度、容灾、缓存等，后端更成熟；

 - 合约运行在节点的EVM里，所以“调用合约” 本质上是向某个节点发送请求/交易；
 - 智能合约运行在 以太坊节点的EVM里；所以你想读写合约，都必须先“连上一个节点”；
 - 后端连接节点两种方式：
    - 同机：可以用 IPC（进程间通信，本地更快更稳）、RPC（HTTP/WebSocket 的远程调用方式）
    - 不同机：只能用 RPC（最常见）



 
